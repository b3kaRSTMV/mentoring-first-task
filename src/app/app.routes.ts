import { Routes } from '@angular/router';
import { UserList } from './components/user-list-component/user-list.component';



export const routes: Routes = [
    {path: 'users', component: UserList},
];
